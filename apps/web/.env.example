# Managed Better Auth (server only)
NEON_AUTH_BASE_URL=https://<your-neon-auth-host>/<database>/auth
NEON_AUTH_COOKIE_SECRET=<generate-at-least-32-random-characters>

# Optional: when the DindIn API has a dev URL, the auth debug page will call GET /v1/me.
NEXT_PUBLIC_DINDIN_API_URL=
